# 收尾清单（v1.1.0 提审前）

> 核对时间：2026-09-10 12:35 ｜ 基准：`git status` 干净（HEAD `b4a2137`）+ EF 部署清单 + 上线前清单（8/13）+ 近两日施工日志
> 说明：旧清单 `上线前_完整手动操作清单_2026-08-13.md` **已过期**（Supabase 迁移是 9/4 之后的事），本清单覆盖其缺口。

## 📌 进度速览（2026-09-10 12:35 更新）

| # | 项 | 状态 |
|---|---|---|
| 1 | 域名白名单（Supabase + steamusercontent） | ✅ **已完成**（用户确认全部添加） |
| 2 | 隐私协议后台同步 | 🟡 **文案已备好，待粘贴**（见 ④） |
| 3 | 云函数 aggregation 上传部署 | ✅ **已完成** |
| 4 | 赛事页耗时验证 | ✅ **已完成** |
| 5 | 首页「即将开始」卡验证 | ✅ **已完成** |
| 6 | `page` 字段链路不一致 | ⏸ 已注释记录（待真机推送验证） |
| 7 | M2.4 剩余 7 action | ☐ 未开始 |
| 8 | GitHub 远程仓库 + Secrets | ☐ 未开始 |

---

## 🔴 P0 — 阻断提审 / 上传

### 1. ✅ 域名白名单（已完成）

~~`utils/supabaseClient.js` 用 **`wx.request` 直连** `https://gkticzdaicpdtxheyxsd.supabase.co`，
开发态勾了「不校验合法域名」所以正常，**体验版/正式版会直接被拦** → 全部 EF 调用失败 →
静默回落云开发（Supabase 迁移白做 + 推送挂掉）。~~

**用户已确认全部添加**（含 `https://cdn.steamusercontent.com`）。

> 备忘：死域**不要加** —— `steamusercontent-a.akamaihd.net`（实测 100% 404）、
> `cloud-3.steamusercontent.com`（http 非 https）。

### 2. 🟡 隐私协议后台同步（文案已备好，待粘贴）

**已完成的准备工作**（2026-09-10 v8.34）：

- ✅ 页内 `privacy.wxml` **合规修正 5 处** —— 原表述与实际代码行为不符（属不实陈述风险）：
  1. 第三条「第三方 API 均由小程序直接发起（不经过自有服务器）」→ 实际经云函数/EF 代理
  2. 第二条「开启赛前通知后同步至云端」→ OpenID 实际 **onLaunch 无条件**获取
  3. 第六条「开启赛前通知时获取 OpenID」→ 同上是启动即获取
  4. 第三条补列 `haglund.dev` 数据源
  5. 第七条与修正后的第二条矛盾 → 改为「第二条所述上传数据」
- ✅ 版本 2.0.0 → **2.1.0**
- ✅ 建立**单点生成机制**防手抄漂移：`npm run privacy:out` / `npm run privacy:check`

**待执行**：

1. 打开 `deliverables/隐私协议_后台粘贴文案.txt` → **全文复制**
2. 小程序后台「设置 → 服务内容声明 → 用户隐私保护指引」→ 粘贴
3. 按 `deliverables/隐私协议后台同步文案_v2.1.0_2026-09-10.md` 的勾选核对表核对「隐私接口声明」
4. 提交审核（1-3 工作日）
5. 提交后跑 `npm run privacy:check` 复核未漂移

> ⚠️ 后台文案与页内文案**必须字节一致**（审核逐字比对），故不做手抄。

---

## 🟠 P1 — 功能验证

| # | 项 | 状态 | 怎么验 |
|---|---|---|---|
| 3 | 云函数 aggregation 上传部署 | ✅ **已完成** | — |
| 4 | 赛事页耗时验证 | ✅ **已完成** | — |
| 5 | 首页「即将开始」卡 | ✅ **已完成** | — |
| 6 | `page` 字段链路不一致 | ⏸ 已记录未改 | 云函数带前导斜杠 / Supabase EF 剥离 —— 无实测依据不动行为，待真机推送时用真实结果统一 |
| 7 | tdesign 组件回填 | ⚠️ | v8.26 回填 4 组件后**未重编译验证**，可能仍有第二批缺失 |
| 8 | STRATZ 链路 | ⚠️ | `stratz.enabled=true` 但日志无 `[stratz] ✓ cloud` → 疑似 Cloudflare 拦云函数出口 IP 复发，**待查云函数日志**（需 STRATZ_API_KEY） |

---

## 🟡 P2 — 架构收尾（M2.4 / 数据源迁移）

### 9. M2.4 剩余 action（18 已迁 / 7 未迁）

**已接 EF（18）**：OpenDota 12 个 + `steamProxy` + `steamLeagueScheduled` + `stratzGql`
+ `liquipediaFetchRawWikitext` + `getLeagueDetailBundle` + `haglundUpcoming`

**未迁（7，仍走云开发）** —— 契约不兼容（EF 是薄代理，云函数侧含解析/聚合逻辑）：

```
liquipediaScheduledMatches   ← 详情页/首页赛程主源，收益最大
liquipediaLeagueMeta
liquipediaTeamLogo
liquipediaListTournaments
getLiquipediaUpcoming
getUpcomingSchedule
health
```

### 10. GitHub 远程仓库缺失 → LP 定时同步永不执行

`.github/workflows/sync-liquipedia.yml` 存在（每日 02:00/14:00 UTC 同步 LP 缓存到 Supabase），
但 **`git remote -v` 为空** → 仓库没推 GitHub → **workflow 永远不会跑**。

要做：① 建 GitHub 仓库并 push ② Settings → Secrets 配
`SUPABASE_URL` + `SUPABASE_SERVICE_KEY` ③ Actions 页手动 Run 一次验证。

> 这是 Liquipedia Cloudflare 拦截的绕行方案（EF 补浏览器指纹头已试失败）。

### 11. 真机推送验证（未做）

- 订阅消息模板状态需在后台核对：**「已通过」**（非审核中/被驳回）
- `smart-reminders` EF 已部署但**客户端零引用（未接线）**
- `page` 字段两条链路传参不一致（见 P1 #6）

---

## 🟢 P3 — 体验遗留（可延后）

| # | 项 |
|---|---|
| 12 | 详情页仍用 `--status-live` 红，项目级统一去红未排期 |
| 13 | 除 league-detail 外其他页面 wxss 可能有字面量色残留 |
| 14 | NEXUS SERIES I 的 `tierLabel` 空（未收录 curation，OpenDota 直出） |
| 15 | GroupTableLeague 的 W/L 由 LPDB 渲染，胜负靠 OpenDota 聚合互补 |

---

## 建议执行顺序（剩余）

```
① 后台粘贴隐私文案 + 提审（#2）   ← P0 唯一剩余阻塞，5 分钟
② 真机跑推送（#11）+ 验 #7 #8
③ GitHub 仓库 + Secrets（#10）
④ M2.4 剩余 7 action（#9）        ← 非阻塞，可排 v1.2
```

---

## 本次收尾已完成的动作

- ✅ 核对 `git status` 干净、HEAD = `b4a2137`（v8.31/v8.32 均已提交）
- ✅ 核对 11 个 EF 全部 `ACTIVE`（`opendota-proxy` v6 = 今日部署）
- ✅ 量化 M2.4 覆盖缺口（18 已迁 / 7 未迁，逐条列出）
- ✅ 识别域名白名单漏 Supabase（旧清单无此项）→ **用户已修复**
- ✅ 识别 GH Actions 因无 remote 而永不执行
- ✅ `page` 字段链路不一致已加注释记录（未改行为）
- ✅ **隐私协议合规修正 5 处** + 单点生成机制（v8.34）

