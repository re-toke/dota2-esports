# ⑥ 云开发下线 · 可勾选确认清单（v2 · 2026-09-15）

> 配套：`阶段6_云开发下线事前确认清单_2026-09-14.md`（背景与理由，本文是可执行的勾选版）
> 图例：✅ 已由我实证（附证据） ｜ ⬜ 需你确认（附「怎么验证」） ｜ ⚠️ 待你决策

---

## 前置状态（今天已实证，无需你复核）

| 项 | 证据 |
|---|---|
| ④ 赛事发现迁 GH Actions | ✅ **已实跑**：curation_events 2414 → **3247**（+833），最近写入 2026-09-15 08:19 UTC |
| `cacheRefresh` 的产出已在 Supabase | ✅ `search_index` / `teams_hot` / `teams_search` 三个 key 均在且**未过期**（剩 ~277 分钟）|
| 三表数据在 Supabase | ✅ `curation_events 3247` / `curation_teams 23` / `curation_meta 1` |
| 管理后台读写 | ✅ EF 鉴权实测 + 写入落库实测（`ecl2013finalstest`）|
| EF 单点监控 | ✅ `scripts/ops/probe-ef.js` + `.github/workflows/ef-health.yml`（每 2h）|

---

## A 组 · 客户端不再依赖云开发

- [ ] **A1** 真机 Console **无**以下兜底日志：
      `[remoteCuration] 云函数返回` / `→ 回落云开发`
  > **怎么验证**：开发者工具 → 清缓存 → 重新编译 → 打开首页/赛事/详情页，Console 筛 `remoteCuration` 与 `回落`。
  > 预期只看到 `走 Supabase 直读` / `首屏走 Supabase 直读·近期子集`。
  > ⚠️ **可接受**的例外：`[liquipedia] EF 缓存未命中或无对阵 → 云函数兜底`（LP 对阵的兜底，设计内）

- [ ] **A2** 首页 / 赛事列表 / 赛事详情 / 战队页 均能正常显示
- [ ] **A3** 关注 / 订阅提醒可用（依赖 openid → EF `wechat-auth`）
- [ ] **A4** 「我的 → 云同步」状态符合预期（默认 off）

---

## B 组 · 三个定时触发器都有替代

| 触发器 | 原职责 | 替代 | 勾选 |
|---|---|---|---|
| `dailyDiscover`（每天 03:00）| 赛事发现 + 分级同步 | **GH `discover-tournaments.yml`** | [x] ✅ **已实跑**（+833 条）|
| `cacheRefresh`（每 6h）| `search_index`/`teams_hot`/`search` 预热 | **GH LP 同步的 `syncIndexes()`** | [x] ✅ **三 key 在且未过期** |
| `schedulePreheat`（每 30min）| `upcoming_schedule` 赛程预热 | ⚠️ **尚无替代** → 见 D 组 | [ ] |

- [ ] **B1** 去 Actions 页确认 `Discover Tournaments` 的**定时**也会跑（不是只有手动触发）
      > 怎么验证：Actions → Discover Tournaments → 看 workflow 文件里 `on.schedule` 存在即可（cron `20 19 * * *` = 北京 03:20）

- [ ] **B2** 去 Actions 页确认 `EF Health Check` 最近一次运行**成功**
      > 怎么验证：Actions → EF Health Check → 最近一条应 ✓（它是 EF 单点的告警通道，必须活着）

---

## C 组 · 数据资产已完整搬走

- [x] **C1** `curation_events` → Supabase ✅（3247 行）
- [x] **C2** `curation_teams` → Supabase ✅（23 行）
- [x] **C3** `curation_meta` → Supabase ✅（1 行，ids[57]）
- [ ] **C4** `aggregation_cache`（云开发侧）→ 仅云函数内部使用，客户端不直接读 ✅ 无需迁
- [ ] **C5** ⚠️ **`admin-logs` 历史记录未迁**
      > 如需留档：微信云开发控制台 → 数据库 → `admin-logs` → 导出 JSON，存到本地/仓库外
      > 是否留档由你决定（我建议导一份，历史告警有排查价值）

---

## D 组 · ⚠️ 唯一没有替代品的 `schedulePreheat`（需你拍板）

它每 30min 写 `upcoming_schedule`，供**首页/列表页「即将」tab 秒开**。

| 选项 | 做法 | 代价 | 选择 |
|---|---|---|---|
| **D1** ⭐ 先不做 | 接受「即将」tab 首次打开慢一点（客户端仍会实时聚合）| 体验轻微降级，功能不坏 | [ ] |
| D2 | 迁到 GH Actions（每 30min workflow → 写 Supabase，客户端改直读）| 要改首页核心数据流 | [ ] |
| D3 | 为此保留云开发 | 与「完全脱离」冲突 | [ ] |

> **我的建议 D1**：先不做，观察期用真实体验数据决定。预热是「锦上添花」，不是功能依赖。

- [ ] **D-final** 我的选择是：______（D1 / D2 / D3）

---

## E 组 · 回退链路失效的取舍（停触发器后即失效）

| 现在失效后 | 影响 |
|---|---|
| `getOpenId`（登录兜底）| EF 挂了 → 无法登录 → 关注/提醒/订阅全废 |
| `sendSubscribeMessage`（订阅兜底）| 同上 |
| `getCached`/`setCached`（KV 兜底）| 无（SB 直读已生效）|
| `liquipediaTeamLogo`（队标兜底）| 三源皆无 logo 的冷门队显示默认图标（已知，影响小）|

- [ ] **E1** 我已接受「EF 成为单点」，并确认监控告警可用（见 B2）
- [ ] **E2** 我理解**停触发器 ≠ 删云函数** —— 前者可逆，后者不可逆

---

## 执行顺序（勾完 A/B/E 后再动手）

```
① 停三个定时触发器      ← 可逆（随时可启用）
   微信开发者工具 → 云开发控制台 → 云函数 → 各函数 → 触发器 → 停用
   顺序：schedulePreheat → cacheRefresh → dailyDiscover(cron-discover-tournaments)
   （dailyDiscover 已实跑替代成功，最放心；schedulePreheat 按 D 组结论处理）

② 观察 3~7 天，盯三件事
   · 真机 Console 有无新的兜底/报错
   · 两个 workflow（discover / ef-health）是否按时成功
   · admin 后台读写是否正常

③ 无异常后才考虑删云函数（先导出两个云函数目录留档）

④ 环境本身通常不必删（闲置成本极低，不调用不产生费用）
```

---

## 一句话总结

**A/B/C 组我都已用数据验证或可自动验证；真正需要你的只有 3 件：**
1. **A1** 真机看一次 Console（确认无云函数兜底）
2. **B2** 去 Actions 看一眼 EF Health Check 是否成功
3. **D-final** 拍板 `schedulePreheat` 怎么办（我建议 D1）

这三件过了，就可以执行「① 停触发器 + 观察」。
