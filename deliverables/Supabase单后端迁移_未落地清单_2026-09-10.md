# Supabase 单后端迁移 · 未落地清单（2026-09-10 全量盘点）

> 方法：对比「云函数 `aggregation` 支持的 31 个 action」与「`EDGE_ACTIONS` 已路由到 EF 的 18 个」，
> 再逐个回客户端确认是否仍被调用，最后核运维侧（GH Actions / cron / EF 接线）。

---

## 一、总体进度

| 维度 | 状态 |
|---|---|
| 站点/出口 | ✅ 已切（`cloudProxy.enabled=true` + `supabase.enabled=true`） |
| EF 数量 | **11 个**（opendota/steam/stratz/liquipedia/haglund/bundle/wechat-auth/follow-profile/subscribe-send/refresh-wx-token/smart-reminders） |
| action 迁移 | **18 已迁 / 13 仍走云开发**（其中 2 个是死代码，3 个只差调换优先级；余为回退与运维） |
| 域名白名单 | ✅ 已加（`gkticzdaicpdtxheyxsd.supabase.co` + `cdn.steamusercontent.com`） |
| 云函数部署 | ✅ 已上传 |

---

## 二、仍走微信云开发的 13 个 action（按可迁性分类）

### A 类：**技术上可迁、目前未做**（6 个 + 1 组 KV）

| action | 用途 | 调用方 | 迁移难度 |
|---|---|---|---|
| ★★ `getCached` / `setCached` | **通用 KV 缓存**（cloudCache 除 `follow_profile_*` 外的全部 key） | `utils/cloudCache.js:77/95/98` | 中 |
| `getSearchIndex` / `buildSearchIndex` | 搜索索引 | `cloudCache.js:107/120` | 中 |
| `getTeamsHot` / `getTeamsIndex` | 热门战队 / 战队索引 | `cloudCache.js:137/156` | 中 |
| `getExperiments` | A/B 实验分流 | `utils/experiment.js` | 低 |
| `getCuration` | 云端策展数据 | `utils/remoteCuration.js` | 低 |
| `health` | 健康检查 | `utils/cloudProxy.js` | 低 |

**为什么 A 类最大的一块（通用 KV）没迁 —— 有个真实约束**：

`follow_profile_*` 能走 EF，是因为它**按 openid 分桶**、可用 JWT 鉴权。
而通用 KV（搜索索引、热门战队等）是**公共数据、不带用户身份**：
- 客户端直连 PostgREST 读 → 需要 `aggregation_cache` 对 anon 开放 → **表里同时存着用户画像**，会泄漏
- 客户端直连写 → 需要 service key → **绝不可放客户端**

→ 所以通用 KV 要迁，**必须新增一个 EF**（如 `cache-kv`，读开放、写校验），无法靠直连解决。
这是它能一直留着的真实原因，不是遗漏。

### B 类：**★ 原判「不可迁」有误 —— 解决方案早已实现，只是从未运行**

> ⚠️ **本清单初稿把 LP 系列判为「架构边界、不可迁」，这个结论下得太粗。**
> 用户指出「我记得有解决方案的」→ 核查确认**方案确实存在且代码已写完**。
>
> **问题出在我的推理**：我把「LP **抓取**不可从 Supabase 出口做（429 实证）」错误地
> 推广成了「LP **服务**不可迁」。这两件事是不同的：
> - **抓取（fetch）**：确实不可 → Supabase 出口 IP 被 LP 429
> - **服务（serve）**：**可以迁** → 抓取交给 **GitHub Actions**（海外出口、IP 信誉好），
>   EF 只负责**读表**，运行时零 LP 依赖

#### 已有但未运行的方案（两件套都在仓库里）

| 组件 | 路径 | 作用 |
|---|---|---|
| 同步脚本 | `scripts/sync-liquipedia-cache.js` | 抓 178 条 slugmap 全量 → 写 `aggregation_cache`（`lp:w:<slug>`，TTL 26h） |
| 定时任务 | `.github/workflows/sync-liquipedia.yml` | 每日 02:00 / 14:00 UTC 跑上面这个脚本 |

**唯一阻塞：仓库无 git remote → workflow 从未执行。** 不是技术不可行。

#### 7 个 action 的真实状态（逐个核过客户端）

| action | 真实状态 | 待做 |
|---|---|---|
| **`liquipediaScheduledMatches`** | ★ 客户端**早已有** EF raw + 本地解析路径（`fetchScheduledLocal` → `LiquiParse.parseScheduledMatches`），只是**排在回退位** | ①表有数据 ②调换优先级 |
| **`liquipediaLeagueMeta`** | ★ 客户端**早已有**本地解析（`getLeagueMetadata` 注释明确写「复用已抓取的 wikitext」→ `LiquiParse.parseLeagueMetadata`） | 同上 |
| **`getUpcomingSchedule`** | ★ 已有本地预构建快照（`upcoming-local.json`）共用同一构造器 | 同上 |
| `liquipediaListTournaments` | ❌ **死代码**：`listAllTournaments` 已于 O-11（2026-08-15）删除，0 业务引用 | **直接删** |
| `getLiquipediaUpcoming` | ❌ **死代码**：无任何客户端调用 | **直接删** |
| `liquipediaPrewarm` | 无客户端调用；EF 侧已预热概念由 GH Actions 承担 | 归 EF / 删 |
| `liquipediaTeamLogo` | 需 LP API 取 logo URL，无本地解析替代；但 `enrichTeamLogo` 已有多源兜底（OpenDota→STRATZ→LP），LP 只是最后兜底 | 可用 EF raw + 解析覆盖（低优先） |

**即：2 个是死代码（删掉即"迁完"），3 个只差「表有数据 + 调换顺序」+ 2 个低优先。**

#### ★★ 2026-09-11 实测补充：**赛程要真走 EF，必须先改 sync 脚本的灌表范围**

落地「不依赖 GitHub」那条路径时，实测发现一个**此前未识别的硬前提**：

| 事实 | 实测数据 |
|---|---|
| 表里存的是 slugmap 的**赛事主页面** slug | 178 条中 173 条含斜杠，样例 `EPL/Masters/2`、`BLAST/SLAM/8/Europe` |
| 但赛程对阵在**子页面** | `getScheduledMatches` 用的 slug 来自 curation 的 `scheduledMatchesSlug`，如 `The_International/2026/Group_Stage` |
| 子页面在表里吗 | **3/3 MISS**（`The_International/2026/Group_Stage` 等全部未收录） |
| 主页面能解析出对阵吗 | **0 场** —— 主页面本就不含 `{{Match}}` 模板 |

→ **结论：只要灌表范围仍是「slugmap 主 slug」，赛程就**不可能**从 EF 表命中**
（无论客户端如何调整优先级）。

**下一步必做（属于 GH Actions 打通的一部分）**：
```
sync 脚本的抓取范围 = slugmap 主 slug  ∪  curation 的 scheduledMatchesSlug
                      （后者才是含 {{Match}} 的对阵页）
```
改完并跑过一次同步后，`fetchLiquipediaChain` 的「本地解析优先」才值得重新启用。

> ⚠️ 本次已把该处**回退为云函数优先**：实测那条路径必然落空，
> 却要在每次云函数调用前先白等 0.5~1.7s（EF cacheOnly 往返）→ 净负收益。

#### 仍然有效的那条**不依赖 GitHub** 的路径

关键在于：EF 的 `liquipediaFetchRawWikitext` **未命中缓存时会去现抓 LP**（→ 429 → 7s 后才 500）。

→ 给 EF 加一个 `cacheOnly` 参数：**未命中立即返回 null（0 延迟，不尝试现抓）**。
这样客户端就能安全地「EF 缓存优先」：

```
尝试 EF（cacheOnly：命中即返回，未命中 <100ms 返回 null）
  ├─ 命中 → 本地解析（快，零云开发调用）
  └─ 未命中 → 回落云函数（可靠）
```

**收益**：热门赛事（表里有的）立刻走 EF 快路径，冷门赛事走云函数兜底 ——
**不等 GitHub 打通就能拿到一半收益，且零回归风险**（未命中只多花 <100ms）。

### C 类：**故意保留为回退链路**（4 个，属设计）

| action | 角色 |
|---|---|
| `getOpenId` | `wechat-auth` EF 失败时回退（`auth.js` `_cloudFallback`） |
| `getFollowProfile` / `saveFollowProfile` | `follow-profile` EF 失败时回退 |
| `sendSubscribeMessage` | `subscribe-send` EF 失败时回退 |

> ⚠️ **回退写入实际是「写进黑洞」**（本次核查新发现，详见第四节 P0）

### D 类：服务端/管理专属（无客户端调用，不需要迁）

`refreshTeams`、`discoverOpenDotaTournaments`、`adminWriteCuration`、`sendSmartReminders`

---

## 三、运维侧未落地（非代码，但会静默失效）

| # | 项 | 现状 | 影响 |
|---|---|---|---|
| 1 | ★★ **GitHub 仓库 + Secrets** | `git remote -v` **为空** → `.github/workflows/sync-liquipedia.yml` **永不执行** | `lp:w:*` 表无人定期灌 → EF 的 LP 缓存路径长期不新鲜（当前靠本地手动跑 sync 脚本维持） |
| 2 | ★ **`smart-reminders` EF 未接线** | 已部署，**客户端零引用**（全仓仅 2 处注释提及） | 服务端批量推送链路形同虚设；当前推送实际走**客户端触发**（首页 `checkPreMatchReminders`） |
| 3 | `cron-job.org` 双 Job | 外部配置，仓库内不可核 | 需到站点确认是否仍指向正确的 EF URL/参数 |
| 4 | `refresh-wx-token` EF | 已部署，客户端零引用 | 由 `subscribe-send`/`smart-reminders` 内部调用，属正常；但**若 smart-reminders 不接线，它的价值减半** |
| 5 | LP 表（`lp:w:*`）保鲜 | 依赖 sync 脚本（GH Actions 不跑） | 与 #1 同源 |

---

## 四、建议的补齐顺序

### P0（1 项，真问题 —— 一致性/数据丢失，非合规）

> ⚠️ **先说一处自我纠正**：本清单初稿曾断言「`cloudCache` 回退路径绕过 `cloudSync.isEnabled()` 守卫，
> 未开启同步仍可能把画像写到云开发」。**核查后此判断为错** ——
> 逐条验过所有 `follow_profile_*` 读写调用点，**全部有同步守卫**：
> `saveFollowProfile`（`subscribe.js:594`）✓ / `syncSubStatus`（`:545`）✓ /
> `restoreFollowFromCloud`（`:635`）✓ / `restoreSubFromCloud`（`:550`，且在 guarded 函数内）✓
>
> 核查过程反而发现了**另一个真问题**（见下）。

**★ 回退写入是「写进黑洞」：写进云开发，但读取端永远读不回**

```
写：EF 失败 → cloudProxy.call('setCached', ..., 'follow_profile_<openid>')
              → 落到【微信云开发 DB】

读：cloudCache.getCached(key)
      → _sbFollowGet(key)（EF → Supabase Postgres）
      → .catch(local)            ← 只 catch 异常，**不查云开发**
      → 且 EF 正常返回 null（Supabase 里确实没有）也不会去查云开发
```

**结论**：回退写入的数据**没有任何读取路径能取回**。
即「EF 失败 → 回退写云开发」这个设计**只提供心理安慰**，实际等于**静默丢数据**：
用户以为保存成功了（接口返回 ok），但下次读是空的。

**影响面**：EF 偶发失败时，用户的关注/提醒策略变更会**静默丢失**（无任何提示），
且**不会自愈**（本地有兜底副本，但换机/清缓存后彻底没了）。

**修法（二选一）**：
- **A（推荐，最小改动）**：回退写入失败时**明确返回 `ok:false`**，让上层提示「同步失败，可重试」——
  即**把静默失败变成显式失败**（与刚修完的 `disable()` 报假成功是同一类问题的另一面）
- **B（彻底）**：读路径补一次云开发查询（`getCached` 失败/未命中时也查云开发）——
  但会让读取变慢且引入双后端读依赖，**不推荐**

### P1（3 项，让迁移真正收尾）

1. **新增 `cache-kv` EF**，接管通用 KV（`getCached`/`setCached` + 搜索索引 + 热门战队）——
   这是剩余最大的一块，做完后**通用缓存不再依赖云开发**
2. **GitHub 仓库 + Secrets** → 让 `sync-liquipedia.yml` 能跑，LP 表保鲜
3. **★ LP 服务迁 EF（B 类真实待做，原判为「不可迁」有误）**：
   - ① EF 加 `cacheOnly` 参数（未命中立即返回，不尝试现抓 → 避免 7s 429 延迟）
   - ② 客户端 `fetchLiquipediaChain`：EF 缓存优先 → 未命中回落云函数
   - ③ `getLeagueMetadata` / `getUpcomingSchedule` 同法
   - ④ **删死代码**：`liquipediaListTournaments`、`getLiquipediaUpcoming`
   - 其中 ①②④ **不依赖 GitHub**，可立即做；③ 收益更大

### P2（3 项，清理与收尾）

3. 迁 `getExperiments` / `getCuration` / `health`（低难度，补齐后仅剩 LP + 回退链路）
4. **决定 `smart-reminders` 的去留**：接线（补服务端推送）或 **明确不接线并删除 EF**（避免「部署了却没用」的认知负担）
5. 核对 `cron-job.org` 双 Job 配置

---

## 五、迁移终点的准确描述

做完 P1 后，架构会是：

```
Supabase EF（数据面）          微信云开发（仅剩）
├─ opendota-proxy              ├─ liquipedia*（7 个，架构边界）
├─ steam-proxy                 ├─ 回退链路（4 个，设计如此）
├─ stratz-proxy                └─ 服务端/管理（4 个）
├─ liquipedia-proxy（读表）
├─ haglund-proxy
├─ bundle-aggregator
├─ cache-kv（新增）
├─ wechat-auth
├─ follow-profile
└─ subscribe-send
```

**★ 更正**：初稿曾写「LP 那 7 个不是没做完，而是实测不可迁」—— **这句话是错的**。
正确说法是：**LP 抓取不可迁（429），但 LP 服务可迁**（抓取交给 GH Actions，EF 读表）。
做完 P1 第 3 项后，LP 系列可进一步降到「仅冷门赛事回落云函数」。

---

## 六、一句话回答「还有哪些未落地」

| 类别 | 数量 | 说明 |
|---|---|---|
| **真问题** | **1** | 回退写入是「写进黑洞」（P0，静默丢数据） |
| **可迁未迁** | 6 + 1 组 KV | 最大一块是通用 KV（需新增 EF） |
| **★ 原判「不可迁」实为可迁** | 7 | 3 个只差「表有数据 + 调换优先级」、**2 个是死代码**、2 个低优先；方案（GH Actions 灌表）已实现但从未运行 |
| **故意保留** | 4 | 回退链路 |
| **不需迁** | 4 | 服务端/管理专属 |
| **运维未落地** | 5 | GH Actions 无 remote / smart-reminders 未接线 / cron 待核 |
