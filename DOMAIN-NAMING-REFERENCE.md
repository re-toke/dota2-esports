# 合法域名构成条件与规范要求

<!-- ARCHIVED-BANNER -->
> ⚠️ **本文档已归档**（最后更新 2026-07-25，项目此后已演进约 2 个月）。
> **请勿以本文作为现状依据。** 证据优先级：**代码 / `git log` > `deliverables/` > 本文 > 整合版**。
> 已核实的两处典型失真：
> · 文中描述的**微信云函数链路（`aggregation` / `upcoming_schedule` 预热等）已彻底退役** —— 现为 **Supabase 单后端**；
> · **「进行中 = 最近 7 天」已改为「2 小时」**（见 `utils/config.js` 的 `leagueWindow`）。
> 权威入口（均在**本仓库内**且最新）：
> · `deliverables/项目复核与优化方案-v2-2026-09-26.md`
> · `deliverables/复核意见-优化方案v2-2026-09-26.md`
> · `deliverables/P0-B-SLO口径与采集点-2026-09-26.md`
> 完整技术文档：`../DOTA2赛事通-项目技术文档-整合版.md`（工作区外 · v8.93 · 2026-09-25）

> 适用场景：微信小程序「服务器域名（合法域名）」配置、自建代理（方案 B）域名注册时参考。
> 通用 DNS 标准为主，末尾专门说明微信小程序的额外门槛。

## 1. 基本结构（FQDN）

完整域名由若干 **label（标签）** 以点 `.` 分隔，自右向左层级升高：

```
api . esports . example . com
 │      │         │        └ 顶级域 TLD
 │      │         └────────── 二级域名 SLD（多数 gTLD 的“可注册单元”）
 │      └──────────────────── 子域名（三级、四级…）
 └─────────────────────────── 子域名
```

- 最右侧是 TLD，向左依次是二级域名、三级、四级……（统称为子域名）。
- 书写与解析都不区分大小写（规范存储为小写）。

## 2. 长度限制（RFC 1035 / 2181）

- **单个 label ≤ 63 字节**。
- **完整 FQDN ≤ 255 字节（含点）**；根标签为空，所以“可见字符”上限约 **253** 个。
- 例：`a.very-long-but-under-63-chars-label.example.com` 各段都要守 63 字节红线。

## 3. 字符集（LDH 规则，RFC 1035 / 1123）

允许字符（LDH = Letters, Digits, Hyphen）：

| 类别 | 字符 | 备注 |
|------|------|------|
| 字母 | `a–z` `A–Z` | 不区分大小写 |
| 数字 | `0–9` | RFC 1123 允许 label **以数字开头**（如 `3com.com` 合法） |
| 连字符 | `-` | **不能位于 label 的首位或末位** |

明确 **不合法** 的：

- 空格、下划线 `_`、点 `.`（除分隔符外）、其他标点。
- 下划线 `_` 虽用于某些 DNS 记录（如 SRV、`_dmarc`），但 **不是合法主机名字符**，HTTP / 微信小程序域名中不可用。
- 非 ASCII 字符（中文、含音标字母等）必须走 IDN（见第 5 节），不能直接写入。

## 4. 子域名层级规则

- RFC **不限制层级深度**，只要每段 ≤63 字节、总长 ≤255 字节即可。
- 实际“可注册单元”由注册局决定：
  - 多数 gTLD（`.com .net .org`）：注册 `example.com`（二级），你可自由创建任意子域 `*.example.com`。
  - 部分 ccTLD 以**二级为注册单元**：`.co.uk`、`.com.cn`、`.com.tw` 等，即你只能注册在指定 SLD 之下（如 `example.co.uk`）。
- **微信小程序「合法域名」不支持通配符 `*`**：必须逐条枚举子域（如 `api.example.com`、`cdn.example.com` 各写一行）。

## 5. 国际化域名（IDN）支持方式

- 标准：IDNA —— IDNA2003（RFC 3490 / 3492）、IDNA2008（RFC 5890–5895）。
- 机制：**按标签（per-label）** 转换。应用层用 **Punycode（RFC 3492）** 把 Unicode 标签编码为以 `xn--` 开头的 ASCII 标签；ToASCII / ToUnicode 负责双向。传输与解析仍走标准 ASCII DNS。
  - 例：`例子.中国` → `xn--fsqu00a.xn--fiqs8s`。
- 约束：编码后 label 仍须 ≤63 字节；浏览器/系统负责转换，用户看到中文。
- 实践建议：微信合法域名配置时优先填 **ASCII（英文）域名**，避免编码差异导致后台校验失败；若必须用中文域名，确认后台接受 Punycode 形式。

## 6. 顶级域（TLD）分类与限制

| 类别 | 示例 | 限制 |
|------|------|------|
| 通用 gTLD | `.com .net .org` + 2013 起新通用 `.app .dev .shop .xyz` | 开放注册，无主体资格限制 |
| 国家/地区 ccTLD | `.cn .us .jp .uk`（ISO 3166-1 α-2 两字母） | 部分需本地资格/实名；`.cn` 需实名认证 + ICP 备案 |
| 赞助 sTLD | `.gov .edu .mil .int .jobs .travel` | 资格限制（政府/教育/军事/行业） |
| 基础架构 | `.arpa` | 不可注册（反向解析等） |
| 特殊用途/保留 | `.localhost .test .example .invalid .onion` | RFC 2606 / 6761 / 7686，**不可注册** |

- 新 gTLD 字符串可更长，但仍受 label ≤63 字节约束。
- ccTLD 本地化规则示例：`.cn` 需中国实名、`.us` 需美国公民/居民关联、`.eu` 需欧盟关联。

## 7. 注册命名约束与保留字

- **RFC 2606 永久保留（测试用，不可注册）**：`.test .example .invalid .localhost`，以及示例 IP 段 `192.0.2.0/24`、`198.51.100.0/24`、`203.0.113.0/24`。
- **注册局/注册商保留**：各 TLD 注册局可保留特定字符串（如 `nic`、国家/地区名、政府/军事术语）；ICANN 新 gTLD 协议常保留地理名称（GAC 建议）及 IANA 保留名。
- **资格/政策限制**：`.gov/.edu/.mil/.int` 限特定主体；`.cn` 等需实名；商标冲突受 UDRP / 统一争议解决约束，sunrise 期优先商标权人。
- **内容/合规**：多数注册商禁止注册明显侵权、淫秽或冒用政府/机构的名称，违规可被注册局冻结或注销。

## 8. 微信小程序「合法域名」的额外门槛（落地关键）

通用 DNS 标准之外，小程序真正卡你的是这些：

1. **必须 HTTPS**（TLS），端口须为默认 443；不支持自定义端口。
2. **必须 ICP 备案**，且域名备案主体须与小程序注册主体一致（或获授权）。
3. **不允许 IP 地址 / `localhost` / `127.0.0.1`**（仅开发阶段可在开发者工具勾选“不校验合法域名”临时绕过）。
4. **逐条配置**：在 mp 后台「开发 → 开发管理 → 开发设置 → 服务器域名」按类别添加（request / socket / upload / download），**不支持通配符**，每类通常有数量上限（约 200 个，以后台当前显示为准）。
5. 微信云函数调用走微信内部通道，**不计入** request 合法域名（这也是云函数方案免域名白名单的原因）。

## 9. 落到本项目（DOTA2 赛事小程序）

- 若走**方案 B 自建代理**：注册一个合规英文域名（如 `api.yourdomain.com`），按上面 LDH 规则起名即可；中文可用 IDN 但建议先用 ASCII。核心工作量在 HTTPS + ICP 备案 + 同主体，而非 DNS 字符集。
- 若想省事：**方案 A（本地快照，已接入）** 与 **微信云函数（零域名白名单）** 都无需自己注册/备案域名。
