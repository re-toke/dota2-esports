# Dependencies
node_modules/

# WeChat mini program npm build output (vendored, not tracked)
miniprogram_npm/

# TRAE spec/agent metadata
.trae/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Generated output from scripts
scripts/output/
scripts/export/

# 开发者工具预览二维码（临时产物，约 25 分钟过期）
preview-*.png

# ★ 本机密钥（2026-09-11 加固，推 GitHub 前必加）
.secrets.local.json
admin/.env
.env

# verify-curation 等 ops 脚本的运行产物（报告，不入库）
scripts/ops/output/

# Supabase CLI 本地状态（机器相关，CLI 自行清理 —— 2026-09-22：此前误提交，导致 CLI 一跑工作区就变脏）
supabase/.temp/

# Supabase CLI 本地状态（机器相关，CLI 自行清理 —— 2026-09-22：此前误提交，导致 CLI 一跑工作区就变脏）

# 临时脚手架（批量替换脚本 / 提交信息文件等，一律 _ 前缀）
scripts/tmp/_*

# 微信开发者工具**私有**配置（libVersion / urlCheck / 编译热重载…都是每台机器不同的设置）
# ⚠️ 提交它 = 把某个人的基础库版本强加给全队（曾因此把全队钉在灰度版 3.17.0 上）
project.private.config.json
