# 方案 B（自建轻量代理）成本分析

<!-- ARCHIVED-BANNER -->
> ⚠️ **本文档已归档**（最后更新 2026-07-25，项目此后已演进约 2 个月）。
> **请勿以本文作为现状依据。** 证据优先级：**代码 / `git log` > `deliverables/` > 本文 > 整合版**。
> 已核实的两处典型失真：
> · 文中描述的**微信云函数链路（`aggregation` / `upcoming_schedule` 预热等）已彻底退役** —— 现为 **Supabase 单后端**；
> · **「进行中 = 最近 7 天」已改为「2 小时」**（见 `utils/config.js` 的 `leagueWindow`）。
> 权威入口（均在**本仓库内**且最新）：
> · `deliverables/项目复核与优化方案-v2-2026-09-26.md`
> · `deliverables/复核意见-优化方案v2-2026-09-26.md`
> · `deliverables/P0-B-SLO口径与采集点-2026-09-26.md`
> 完整技术文档：`../DOTA2赛事通-项目技术文档-整合版.md`（工作区外 · v8.93 · 2026-09-25）

> 背景：DOTA2 赛事小程序的「即将到来」tab 需要实时跟随 Liquipedia。已落地两条路——
> 方案 A 本地快照（`utils/upcoming-local.json`，零服务端，发布期快照）、
> 方案 C 微信云函数（`cloudfunctions/aggregation` 的 `fetchLiquipediaUpcoming`，免域名/免备案/内网快）。
> 本文分析方案 B（自建轻量代理，不绑微信云）的真实成本，用于决策是否值得走。

## 一、硬成本（年度，人民币）

| 项目 | A 本地快照 | C 微信云函数 | B 自建代理 |
|---|---|---|---|
| 域名年费 | ¥0 | ¥0 | ¥30–75（.cn 约 ¥29/年，.com 约 ¥55–75/年） |
| 服务器/托管 | ¥0 | ¥0（免费额度内） | ¥0（国内函数免费额度）~ ¥720–1080（轻量服务器，约 ¥60–90/月） |
| HTTPS 证书 | 不需要 | 不需要 | 免费（Let's Encrypt / 云厂 / Cloudflare 自带） |
| **合计/年** | **¥0** | **¥0** | **¥50 ~ ¥770** |

> 微信云开发基础版 0 元/月含免费额度（调用次数、运行时长），本场景日调用数次，远在额度内，故 C 实际为 ¥0。

## 二、隐性成本（比钱更贵）

1. **ICP 备案（仅 B 需要）**：免费但强制。国内任何对外域名须备案，管局审核 1–3 周，需云服务商备案系统 + 主体资质（企业需营业执照）。等待期无法使用。
2. **request 合法域名（仅 B 需要）**：mp 后台逐条登记（不支持通配符），每类有数量上限。云函数走微信内网通道免此步。
3. **开发工作量**：B 几乎零新增——云函数 `parseLiquipediaDate` / `fetchLiquipediaUpcoming` 解析逻辑可直接移植到 Node / Cloudflare Worker，约 0.5–1 天。
4. **运维**：函数托管近乎零运维；轻量服务器需系统更新、监控、证书续期（可脚本化）。

## 三、B 方案的"边际收益"评估

对「即将到来实时跟随 Liquipedia」这一需求，**已实现的云函数方案（C）在能力与成本上几乎覆盖 B**：
- 同样免域名、免备案、内网访问快；
- 同样实时（6h 缓存热更，无需发版）；
- 代码已就绪，仅缺开通云环境 + 上传部署。

B 唯一真正卖点 = **不绑微信云**（已有自有服务器 / 多端复用同一后端 / 抗拒微信云依赖）。否则只多添：一笔域名年费 + 一次备案 + 一份自管运维。

## 四、结论与建议

- **只想让 tab 实时** → 部署已做好的云函数（见 `DEPLOY.md`），最省事。
- **坚决不用微信云** → 走 B「函数版」（国内云函数 + 备案域名），年约 ¥50、开发 1 天内，最轻量不绑云。
- **连备案都不想碰** → 用已接好的本地快照 A（¥0、零服务端），代价是非实时（发布期快照，赛事改期需重跑 `scripts/fetch-liquipedia-upcoming.js` 并重新发布）。

## 五、若决定走 B 的最小可行栈

- 域名：注册 `.cn` 或 `.com`（¥30–75/年），并完成 ICP 备案。
- 托管：腾讯云 SCF / 阿里云 FC（按量，免费额度覆盖），绑定已备案自定义域名。
- 代码：移植 `cloudfunctions/aggregation/index.js` 的 `fetchLiquipediaUpcoming` + `parseLiquipediaDate` 到 HTTP 函数，保留合规 UA（`DOTA2-Esports-Hub/1.0 ...`）、gzip、6h 缓存、Tier≤2 过滤、未来赛事过滤。
- mp 后台：在「开发 → 开发管理 → 服务器域名 → request 合法域名」登记该域名（HTTPS 443）。
- 客户端：将 `leagues.js` 的 `tryCloudUpcoming` 指向自建域名（或新增 `trySelfHostedUpcoming`），其余 `buildUpcomingCard` / `mergeCurationUpcoming` 复用。
