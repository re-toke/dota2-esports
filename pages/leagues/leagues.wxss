.container { padding: var(--space-3); }
/* .with-tab-bar 底部 padding 已在 app.wxss 全局抬到 160rpx（2026-07-29 修复 teams/follow 底部被 tabBar 遮挡），
   leagues 此处不再覆写。 */

/* 底部 spacer：配合 with-tab-bar 留出自定义 tabBar 区域 */
.bottom-spacer {
  height: 24rpx;
}

.skeleton-wrap { margin-bottom: var(--space-3); }

.league-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  position: relative;
  overflow: hidden;
  background: var(--card-grad);
  border-radius: var(--radius-lg);
  padding: var(--space-3) var(--space-3) var(--space-3) var(--space-4);
  margin-bottom: var(--space-2);
  box-shadow: var(--card-inner-shadow);
}
/* 左侧等级彩条（聚光灯：Tier 色浸染渐变——色带 + 向右浸染衰减）
   色标定义在 app.wxss 全局 --card-glow-* Token（v11）。
   伪元素铺满整卡宽度，渐变在 280rpx 处自然衰减到透明，不遮挡内容 */
.league-item::before {
  content: '';
  position: absolute;
  left: 0; top: 0; bottom: 0;
  width: 100%;
  z-index: 0;
}
/* Tier 浸染：覆盖全局 .tier-*::before 的纯双色渐变（优先级更高） */
.league-item.tier-sss::before { background: var(--card-glow-sss); }
.league-item.tier-s::before   { background: var(--card-glow-s); }
.league-item.tier-a::before   { background: var(--card-glow-a); }
.league-item.tier-b::before   { background: var(--card-glow-b); }
.league-item.tier-c::before   { background: var(--card-glow-c); }
/* 内容层置顶，确保文字在浸染渐变之上 */
.league-item .league-main,
.league-item .league-badges,
.league-item .league-sub,
.league-item .league-countdown { position: relative; z-index: 1; }
/* 进行中（LIVE）：绿色光晕替代 Tier 色，卡片亮一级 + 脉冲呼吸（pulse-glow 定义在 app.wxss） */
.league-item--ongoing {
  background: linear-gradient(135deg, #222228 0%, #1e1e24 100%);
  box-shadow: 0 0 16rpx rgba(95,211,95,0.08), var(--card-inner-shadow);
  animation: pulse-glow var(--dur-breath) var(--ease-in-out) infinite;
}
.league-item--ongoing::before { background: var(--card-glow-live); }
.league-item--ongoing .league-name { color: #e8fff0; }
/* 已结束：分元素衰减（文字 85% 可读 / 装饰 45% 渐暗） */
.league-item--ended { opacity: 0.85; }
.league-item--ended::before { opacity: 0.45; }
.league-item--ended .league-badges { opacity: 0.45; }
.league-item--ended .countdown-text { opacity: 0.45; }

.league-main { flex: 1; min-width: 0; }

.league-name-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-1-5);
  flex-wrap: wrap;
}
.league-name {
  font-size: var(--fs-emphasis);
  font-weight: 600;
  color: #f0f3f6;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  flex: 1;
  min-width: 0;
}

.league-badges {
  display: flex;
  align-items: center;
  gap: var(--space-1);
  flex-shrink: 0;
}

.league-meta {
  display: flex;
  align-items: center;
  gap: var(--space-1-5);
  margin-top: 10rpx;
  flex-wrap: wrap;
}

.league-meta .src { font-size: var(--fs-caption); }

/* 状态徽标 */
.status-tag {
  font-size: var(--fs-micro);
  padding: 2rpx 10rpx;
  border-radius: 6rpx;
  border: 1rpx solid;
  line-height: var(--lh-relaxed);
}

/* 第二行 */
.league-sub {
  margin-top: var(--space-1);
  font-size: var(--fs-caption);
}
.date-range { color: #9bb0c9; }

/* 即将到来：倒计时高亮 */
.league-countdown {
  margin-top: var(--space-1);
  display: inline-flex;
  align-items: center;
  background: rgba(95, 211, 95, 0.10);
  border: 1rpx solid rgba(95, 211, 95, 0.3);
  border-radius: var(--radius-pill, 999rpx);
  padding: var(--space-0-5) var(--space-2);
}
.countdown-text {
  font-size: var(--fs-caption);
  font-weight: 600;
  color: #5fd35f;
  letter-spacing: 0.5rpx;
}

/* 来源徽标行 */
.league-source-row {
  margin-top: var(--space-1);
  display: flex;
  align-items: center;
  gap: var(--space-1);
}

/* 底部新鲜度 */
.fresh-bottom {
  text-align: center;
  padding: var(--space-2) 0 var(--space-1);
}

.star-btn { padding: var(--space-1) 0 var(--space-1) var(--space-2); min-width: 88rpx; min-height: 88rpx; display: inline-flex; align-items: center; justify-content: flex-start; box-sizing: border-box; }

/* ===== 2026-08-07（v1.3/v1.4）：底部提示条——暖灰渐变融入列表、金色箭头点缀 ===== */
.loadmore-bar {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
  margin: var(--space-3-5) auto var(--space-1-5);
  padding: var(--space-2-5) var(--space-5);
  width: fit-content;
  max-width: 520rpx;
  background: var(--card-grad);
  box-shadow: var(--card-inner-shadow);
  border: 1rpx solid #262628;
  border-radius: var(--radius-pill);          /* 胶囊形 */
  transition: transform 0.12s ease;
}
.loadmore-bar--loading {
  /* 加载中态：去掉点击反馈，降低视觉重量 */
  opacity: 0.7;
  border-style: dashed;
}
/* v1.4：ARMED 确认态——金色边框高亮 + 呼吸动画，提示「可加载」 */
.loadmore-bar--armed {
  border: 1rpx solid rgba(255, 209, 92, 0.6);
  box-shadow: inset 0 2rpx 0 rgba(0,0,0,0.12), inset 0 -2rpx 0 rgba(0,0,0,0.08), 0 0 16rpx rgba(255, 209, 92, 0.12);
  animation: lmArmedGlow var(--dur-sway) var(--ease-in-out) infinite;
}
@keyframes lmArmedGlow {
  0%, 100% { border-color: rgba(255, 209, 92, 0.35); }
  50% { border-color: rgba(255, 209, 92, 0.85); }
}
.lm-text {
  font-size: var(--fs-label);
  color: #9aa4b2;
  letter-spacing: 0.5rpx;
}
.lm-text--armed {
  color: #FFD15C;
  font-weight: 500;
}
.lm-text--loading {
  color: #5b626d;
}
.lm-arrow {
  display: flex;
  align-items: center;
  justify-content: center;
  /* 金色渐变点缀，呼应项目 SSS tier 色（#FFD15C → #C8A951） */
  filter: drop-shadow(0 0 4rpx rgba(255, 209, 92, 0.15));
  animation: lmArrowBob var(--dur-sway) var(--ease-in-out) infinite;
}
@keyframes lmArrowBob {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(4rpx); }
}
/* 旋转加载圈（纯 CSS 无图） */
.lm-spinner {
  width: 28rpx;
  height: 28rpx;
  border: 3rpx solid #2e2e30;
  border-top-color: #FFD15C;
  border-radius: 50%;
  animation: lmSpin 0.7s linear infinite;
}
@keyframes lmSpin {
  to { transform: rotate(360deg); }
}

/* 即将到来懒加载进度 */
.progress-box {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 100rpx var(--space-5);
}
.spinner {
  width: 56rpx;
  height: 56rpx;
  border: 4rpx solid #262628;
  border-top-color: #5fd35f;
  border-radius: 50%;
  animation: spin 0.9s linear infinite;
  margin-bottom: var(--space-3);
}
@keyframes spin { to { transform: rotate(360deg); } }
.progress-text { font-size: var(--fs-subhead); color: #e6edf3; margin-bottom: var(--space-1-5); }
.progress-tip { font-size: var(--fs-caption); color: #6b7280; text-align: center; }

/* 即将到来为空且 STRATZ 未启用时的引导卡片 */
.empty-hint {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: var(--space-5) var(--space-4) 60rpx;
}
.hint-card {
  width: 100%;
  margin-top: var(--space-3);
  padding: var(--space-4) var(--space-3-5);
  background: #1b222c;
  border: 1rpx solid #2d3744;
  border-left: 6rpx solid #ffcf5c;
  border-radius: var(--radius-lg);
  box-sizing: border-box;
}
.hint-title {
  font-size: var(--fs-emphasis);
  font-weight: 600;
  color: #ffcf5c;
  margin-bottom: var(--space-2);
}
.hint-desc {
  font-size: var(--fs-label);
  color: #9aa4b2;
  line-height: var(--lh-relaxed);
  margin-bottom: var(--space-2-5);
}
.hint-step {
  font-size: var(--fs-body);
  color: #7d8794;
  line-height: var(--lh-loose);
  margin-top: 10rpx;
}
.hint-code {
  font-family: monospace;
  color: #5fd3a8;
  background: #131316;
  padding: 2rpx 10rpx;
  border-radius: 6rpx;
  font-size: var(--fs-caption);
}

/* ===== 等级筛选条（第二行）· 品牌色渐变芯片 ===== */
.grade-bar {
  display: flex;
  justify-content: center;
  gap: var(--space-2);
  margin-bottom: var(--space-3);
  padding: 0 var(--space-0-5);
}
/* 芯片基础：暗底 + 细边框 + 品牌色描边 */
.gc {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  height: 64rpx;
  padding: 0 var(--space-3-5);
  border-radius: var(--radius-lg);
  font-size: 0;
  background: #1a1a1e;
  border: 1.5rpx solid #2d3748;
  cursor: pointer;
  overflow: hidden;
  transition:
    transform 0.20s cubic-bezier(.25,.46,.45,.94),
    border-color 0.22s ease,
    box-shadow   0.22s ease,
    background   0.25s ease;
}
/* hover 微缩放反馈 */
.gc:active {
  transform: scale(0.95);
}
/* 标签文字 */
.gc__label {
  font-size: var(--fs-label);
  font-weight: 700;
  letter-spacing: 2rpx;
  color: #8b949e;
  transition: color 0.22s ease, text-shadow 0.22s ease;
  line-height: 1;
}

/* ── 各等级默认态（未选中）：淡品牌色边框 + 淡文字 ── */
.gc--all   { border-color: #3d4a5c; }
.gc--s     { border-color: var(--tier-s-line); }
.gc--a     { border-color: var(--tier-a-line); }
.gc--b     { border-color: var(--tier-b-line); }

.gc--s   .gc__label { color: var(--tier-s); }
.gc--a   .gc__label { color: var(--tier-a); }
.gc--b   .gc__label { color: var(--tier-b); }

/* ── 选中态：渐变背景 + 发光阴影 + 白/深色高对比文字 ── */
.gc--active {
  border-color: transparent;
  box-shadow:
    0 4rpx 16rpx rgba(0,0,0,.30),
    0 0 20rpx var(--gc-glow, rgba(255,255,255,.08));
  transform: translateY(-2rpx);
}
.gc--active:active {
  transform: scale(0.97) translateY(0);
}

/* ALL 选中：中性亮灰 */
.gc--all.gc--active {
  background: linear-gradient(135deg, #3d4a5c 0%, #2d3748 100%);
  box-shadow:
    0 4rpx 16rpx rgba(0,0,0,.30),
    0 0 18rpx rgba(100,120,150,.15);
}
.gc--all.gc--active .gc__label {
  color: #e6edf3;
  text-shadow: 0 1rpx 3rpx rgba(0,0,0,.35);
}

/* S 选中：翠绿渐变 */
.gc--s.gc--active {
  --gc-glow: rgba(95,211,95,.28);
  background: linear-gradient(135deg, #5fd35f 0%, #2ecc71 100%);
  box-shadow:
    0 4rpx 18rpx rgba(46,204,113,.38),
    0 0 24rpx rgba(95,211,95,.22);
}
.gc--s.gc--active .gc__label {
  color: #0a1f0a;
  text-shadow: 0 1rpx 2rpx rgba(95,211,95,.5);
}

/* A 选中：橙金渐变 */
.gc--a.gc--active {
  --gc-glow: rgba(240,170,40,.28);
  background: linear-gradient(135deg, #f0aa28 0%, #e67e22 100%);
  box-shadow:
    0 4rpx 18rpx rgba(230,126,34,.38),
    0 0 24rpx rgba(240,170,40,.22);
}
.gc--a.gc--active .gc__label {
  color: #1a0f00;
  text-shadow: 0 1rpx 2rpx rgba(240,170,40,.5);
}

/* B 选中：钢蓝渐变 */
.gc--b.gc--active {
  --gc-glow: rgba(155,176,201,.28);
  background: linear-gradient(135deg, #9bb0c9 0%, #7f8fa4 100%);
  box-shadow:
    0 4rpx 18rpx rgba(127,143,164,.38),
    0 0 24rpx rgba(155,176,201,.22);
}
.gc--b.gc--active .gc__label {
  color: #0d1117;
  text-shadow: 0 1rpx 2rpx rgba(155,176,201,.5);
}

/* 来源高亮徽标（Valve 官方 / 顶级第三方） */
.hl-badge {
  font-size: var(--fs-micro);
  padding: 2rpx 10rpx;
  border-radius: 6rpx;
  line-height: var(--lh-relaxed);
  font-weight: 600;
  flex-shrink: 0;
}
.hl-valve { color: #ffcf5c; background: rgba(255, 207, 92, 0.12); border: 1rpx solid rgba(255, 207, 92, 0.4); }
.hl-tp    { color: #5fd3ff; background: rgba(95, 211, 255, 0.12); border: 1rpx solid rgba(95, 211, 255, 0.4); }

/* 已停办徽标（灰） */
.defunct-tag { color: #8a8f99 !important; border-color: #5b626d !important; background: rgba(91, 98, 109, 0.10); }

/* 已停办赛事归档区 */
.archive-block { margin-top: var(--space-3); }
.archive-head {
  display: flex;
  align-items: center;
  gap: var(--space-1-5);
  margin: var(--space-1) var(--space-0-5) var(--space-2);
}
.archive-title { font-size: var(--fs-label); font-weight: 600; color: #8a8f99; }
.archive-count {
  font-size: var(--fs-micro);
  color: #121015;
  background: #8a8f99;
  border-radius: var(--radius-pill);
  padding: 0 var(--space-1-5);
}
/* 归档项整体置灰 */
.league-item.is-defunct { opacity: 0.62; }
.league-item.is-defunct .league-name { color: #aeb6c2; }

/* ===== 焦点卡：重点运营节点（TI15 2026 上海） ===== */
.focus-card {
  position: relative;
  overflow: hidden;
  margin-bottom: var(--space-3);
  padding: var(--space-3-5) var(--space-3-5) 26rpx;
  border-radius: 20rpx;
  background:
    radial-gradient(120% 140% at 0% 0%, rgba(255,207,92,0.16) 0%, rgba(255,207,92,0) 55%),
    linear-gradient(135deg, #1c2230 0%, #11161d 100%);
  border: 1.5rpx solid rgba(255,207,92,0.45);
  box-shadow: 0 6rpx 24rpx rgba(0,0,0,.32), 0 0 24rpx rgba(255,207,92,.12);
}
.focus-glow {
  position: absolute;
  top: -60rpx; right: -60rpx;
  width: 220rpx; height: 220rpx;
  background: radial-gradient(circle, rgba(255,207,92,0.30) 0%, rgba(255,207,92,0) 70%);
  pointer-events: none;
}
.focus-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.focus-badges { display: flex; align-items: center; gap: 10rpx; flex-wrap: wrap; }
.focus-valve {
  font-size: var(--fs-micro); font-weight: 700;
  color: #1a1400; background: linear-gradient(135deg, #ffcf5c, #f0a020);
  padding: 3rpx var(--space-1-5); border-radius: 6rpx;
}
.focus-flag {
  font-size: var(--fs-micro); font-weight: 600;
  color: #ffcf5c; border: 1rpx solid rgba(255,207,92,0.5);
  padding: 2rpx 10rpx; border-radius: 6rpx;
}
.focus-live {
  font-size: var(--fs-micro); font-weight: 700; color: #ff5b5b;
  background: rgba(255,91,91,0.12); border: 1rpx solid rgba(255,91,91,0.45);
  padding: 2rpx 10rpx; border-radius: 6rpx;
}
.focus-star { padding: 0 0 0 var(--space-2); }
.focus-title {
  margin-top: 18rpx;
  font-size: var(--fs-mega); font-weight: 800; color: #ffcf5c;
  letter-spacing: 0.5rpx;
  text-shadow: 0 2rpx 8rpx rgba(255,207,92,.18);
}
.focus-sub {
  margin-top: 6rpx;
  font-size: var(--fs-body); color: #c3ccd9;
}
.focus-meta {
  margin-top: 14rpx;
  display: flex; align-items: center; gap: 14rpx; flex-wrap: wrap;
}
.focus-date { font-size: var(--fs-body); color: #9bb0c9; }
.focus-prize {
  font-size: var(--fs-caption); font-weight: 600; color: #5fd3a8;
  background: rgba(95,211,168,0.10); border: 1rpx solid rgba(95,211,168,0.35);
  padding: 2rpx var(--space-1-5); border-radius: 6rpx;
}
.focus-countdown {
  margin-top: 18rpx;
  display: inline-flex; align-items: baseline; gap: var(--space-1);
  background: rgba(255,207,92,0.10);
  border: 1rpx solid rgba(255,207,92,0.35);
  border-radius: var(--radius-pill);
  padding: var(--space-1) 22rpx;
}
.cd-num { font-size: var(--fs-mega); font-weight: 800; color: #ffcf5c; line-height: 1; }
.cd-unit { font-size: var(--fs-body); color: #e6c97a; }
.focus-countdown--live { background: rgba(255,91,91,0.12); border-color: rgba(255,91,91,0.45); }
.cd-live { font-size: var(--fs-subhead); font-weight: 700; color: #ff7b7b; }
.focus-cta {
  margin-top: 18rpx;
  font-size: var(--fs-body); color: #8b95a4;
  letter-spacing: 0.5rpx;
}

/* 进行中态：红金边框，突出直播氛围 */
.focus-card--live {
  border-color: rgba(255,91,91,0.50);
  box-shadow: 0 6rpx 24rpx rgba(0,0,0,.32), 0 0 24rpx rgba(255,91,91,.14);
}

/* ===== 战队筛选入口（spec A）===== */
.team-filter-row {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  margin: var(--space-2) var(--space-0-5) var(--space-0-5);
}
.team-filter-btn {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  padding: 10rpx 22rpx;
  background: var(--card-bg, #1a1a1e);
  border: 1rpx solid var(--td-border-level-1-color, #262628);
  border-radius: var(--radius-pill);
  font-size: var(--fs-label);
  color: #c3ccd9;
}
.team-filter-btn--active {
  color: var(--dota-gold, #C8A951);
  border-color: rgba(200,169,81,0.5);
  background: rgba(200,169,81,0.10);
}
.tf-label { font-weight: 600; }
.tf-badge {
  min-width: 30rpx;
  height: 30rpx;
  padding: 0 var(--space-1);
  border-radius: var(--radius-pill);
  background: var(--dota-gold, #C8A951);
  color: #121015;
  font-size: var(--fs-micro);
  font-weight: 800;
  line-height: 30rpx;
  text-align: center;
}
.team-clear {
  font-size: var(--fs-body);
  color: #ff7b7b;
}
.team-hint {
  font-size: var(--fs-caption);
  color: #6b7280;
}

/* ===== 战队筛选弹层（spec A）===== */
.tf-mask {
  position: fixed;
  left: 0; top: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.55);
  z-index: 100;
  display: flex;
  align-items: flex-end;
}
.tf-sheet {
  width: 100%;
  max-height: 78vh;
  background: var(--card-bg, #1a1a1e);
  border-radius: 28rpx 28rpx 0 0;
  padding: var(--space-3-5) var(--space-3-5) calc(28rpx + env(safe-area-inset-bottom));
  display: flex;
  flex-direction: column;
  box-shadow: 0 -8rpx 32rpx rgba(0,0,0,.4);
}
.tf-sheet-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-2-5);
}
.tf-sheet-title { font-size: var(--fs-emphasis); font-weight: 700; color: #f0f3f6; }
.tf-sheet-tools { display: flex; gap: var(--space-3); }
.tf-tool { font-size: var(--fs-label); color: var(--dota-gold, #C8A951); }
.tf-list {
  flex: 1;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
}
.tf-cell {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: 22rpx var(--space-1-5);
  border-bottom: 1rpx solid rgba(255,255,255,0.04);
}
.tf-cell--checked { background: rgba(200,169,81,0.06); }
.tf-check {
  width: 40rpx;
  height: 40rpx;
  border-radius: 10rpx;
  border: 2rpx solid #3a424d;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.tf-check--on {
  background: var(--dota-gold, #C8A951);
  border-color: var(--dota-gold, #C8A951);
}
.tf-cell-tag {
  width: 64rpx;
  height: 48rpx;
  border-radius: 10rpx;
  background: var(--card-bg-inset, #131316);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--fs-caption);
  font-weight: 800;
  color: #9aa4b2;
  flex-shrink: 0;
}
.tf-cell-name {
  font-size: var(--fs-subhead);
  color: #e6ebf2;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.tf-empty {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60rpx var(--space-5);
  gap: var(--space-1-5);
}
.tf-empty-text { font-size: var(--fs-subhead); color: #c3ccd9; font-weight: 600; }
.tf-empty-sub { font-size: var(--fs-body); color: #6b7280; text-align: center; }
.tf-sheet-foot {
  display: flex;
  gap: var(--space-2-5);
  margin-top: var(--space-3);
}
.tf-btn {
  flex: 1;
  height: 84rpx;
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--fs-emphasis);
  font-weight: 700;
}
.tf-btn--cancel {
  background: var(--card-bg-inset, #131316);
  color: #9aa4b2;
}
.tf-btn--confirm {
  background: var(--dota-gold, #C8A951);
  color: #121015;
}

/* ===== 首页「我的关注」横向卡片流（spec C.2.1）===== */
.follow-flow {
  margin: var(--space-2-5) 0 var(--space-0-5);
}
.ff-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--space-0-5) var(--space-1) 14rpx;
}
.ff-title {
  font-size: var(--fs-emphasis);
  font-weight: 700;
  color: #f0f3f6;
  letter-spacing: 0.5rpx;
}
.ff-more {
  font-size: var(--fs-display);
  color: #6b7280;
  line-height: 1;
}
.ff-scroll {
  width: 100%;
  white-space: nowrap;
}
.ff-track {
  display: inline-flex;
  gap: 18rpx;
  padding: var(--space-0-5) var(--space-1) var(--space-1-5);
}
.ff-card {
  position: relative;
  overflow: hidden;
  display: inline-flex;
  flex-direction: column;
  width: 300rpx;
  padding: var(--space-2-5) 22rpx var(--space-2-5) 30rpx;
  background: var(--card-bg, #1a1a1e);
  border: 1rpx solid var(--td-border-level-1-color, #262628);
  border-radius: 18rpx;
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,.22);
}
.ff-card::before {
  content: '';
  position: absolute;
  left: 0; top: 0; bottom: 0;
  width: 6rpx;
  background: var(--td-border-level-1-color, #262628);
}
.ff-card.tier-sss::before { background: var(--tier-sss); }
.ff-card.tier-s::before { background: var(--tier-s); }
.ff-card.tier-a::before { background: var(--tier-a); }
.ff-card.tier-b::before { background: var(--tier-b); }
.ff-card-top {
  display: flex;
  align-items: center;
  gap: 10rpx;
  margin-bottom: 14rpx;
}
.ff-tier {
  font-size: var(--fs-micro);
  font-weight: 700;
  color: #121015;
  background: var(--dota-gold, #C8A951);
  padding: 2rpx 10rpx;
  border-radius: var(--radius-sm);
  flex-shrink: 0;
}
.ff-league {
  font-size: var(--fs-body);
  color: #9aa4b2;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.ff-vs {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  margin-bottom: 14rpx;
}
.ff-team {
  font-size: var(--fs-emphasis);
  font-weight: 800;
  color: #e6ebf2;
  max-width: 110rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.ff-team.win { color: var(--h2h-a, #2DD4A7); }
.ff-vs-sym {
  font-size: var(--fs-caption);
  color: #6b7280;
  font-weight: 700;
}
.ff-meta {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10rpx;
}
.ff-cd {
  font-size: var(--fs-body);
  font-weight: 700;
  color: var(--dota-gold, #C8A951);
  font-family: var(--font-numeric, sans-serif);
  letter-spacing: 0.5rpx;
}
.ff-ended {
  font-size: var(--fs-caption);
  font-weight: 700;
}
.ff-ended.win { color: var(--h2h-a, #2DD4A7); }
.ff-ended.lose { color: var(--h2h-b, #F25C54); }
.ff-date {
  font-size: var(--fs-micro);
  color: #6b7280;
  flex-shrink: 0;
}
.ff-skeleton-row {
  display: flex;
  gap: 18rpx;
  padding: var(--space-0-5) var(--space-1) var(--space-1-5);
}
.ff-skeleton-card {
  width: 300rpx;
  height: 168rpx;
  border-radius: 18rpx;
  background: var(--card-bg-elevated, #222226);
  flex-shrink: 0;
}
.ff-empty {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  padding: var(--space-3-5) var(--space-2);
  background: var(--card-bg, #1a1a1e);
  border-radius: var(--radius-lg);
  border: 1rpx dashed var(--td-border-level-1-color, #262628);
}
.ff-empty-text { font-size: var(--fs-label); color: #c3ccd9; font-weight: 600; }
.ff-empty-sub { font-size: var(--fs-caption); color: #6b7280; }

/* 未关注态引导卡 */
.follow-guide {
  display: flex;
  align-items: center;
  gap: 18rpx;
  margin: var(--space-2-5) 0 var(--space-0-5);
  padding: 22rpx var(--space-3);
  background: linear-gradient(135deg, rgba(200,169,81,0.10), rgba(200,169,81,0.03));
  border: 1rpx solid rgba(200,169,81,0.28);
  border-radius: 18rpx;
}
.fg-icon { font-size: var(--fs-mega); }
.fg-text { flex: 1; display: flex; flex-direction: column; gap: var(--space-0-5); }
.fg-title { font-size: var(--fs-subhead); font-weight: 700; color: #f0f3f6; }
.fg-sub { font-size: var(--fs-caption); color: #9aa4b2; }
.fg-btn {
  padding: var(--space-1-5) var(--space-3-5);
  background: var(--dota-gold, #C8A951);
  color: #121015;
  font-size: var(--fs-label);
  font-weight: 700;
  border-radius: var(--radius-pill);
}

/* ===== 1.2 视图切换 + 周轴视图 ===== */
.view-mode-bar {
  display: flex;
  gap: var(--space-2);
  padding: 0 var(--space-0-5) var(--space-1);
}
.vm-spacer { flex: 1; }
.vm-chip {
  display: flex;
  align-items: center;
  gap: var(--space-1);
  flex: 0 0 auto;
  justify-content: center;
  padding: 14rpx 22rpx;
  border-radius: 14rpx;
  background: #1a1a1e;
  border: 1rpx solid rgba(255,255,255,0.08);
  color: #9aa4b2;
  font-size: var(--fs-label);
}
.vm-chip--active {
  background: rgba(200,169,81,0.16);
  border-color: rgba(200,169,81,0.5);
  color: #e8eef6;
}
.wk-scroll {
  width: 100%;
  white-space: nowrap;
  margin-top: var(--space-1);
}
.wk-row {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  flex-wrap: nowrap;
  width: max-content;
  padding-bottom: var(--space-1);
}
.wk-col {
  display: inline-flex;
  flex-direction: column;
  flex-shrink: 0;
  width: 300rpx;
  margin-right: var(--space-2);
  background: #1a1a1e;
  border: 1rpx solid rgba(255,255,255,0.08);
  border-radius: var(--radius-lg);
  padding: var(--space-1-5);
  vertical-align: top;
}
.wk-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-bottom: var(--space-1);
  margin-bottom: var(--space-1);
  border-bottom: 1rpx solid rgba(255,255,255,0.08);
}
.wk-label {
  font-size: var(--fs-caption);
  color: #c9d1d9;
  font-weight: 600;
}
.wk-count {
  font-size: var(--fs-micro);
  color: #C8A951;
  background: rgba(200,169,81,0.14);
  border-radius: var(--radius-pill);
  padding: 2rpx var(--space-1-5);
}
.wk-item {
  padding: 10rpx var(--space-1);
  border-left: 4rpx solid transparent;
  border-radius: var(--radius-sm);
}
.wk-item:nth-child(even) { background: rgba(255,255,255,0.02); }
.wk-item-name {
  display: block;
  font-size: var(--fs-body);
  color: #e8eef6;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.wk-item-meta {
  display: flex;
  align-items: center;
  gap: 10rpx;
  margin-top: var(--space-0-5);
}
.wk-tag {
  font-size: var(--fs-badge);
  color: #8a8f99;
}
.wk-date {
  font-size: var(--fs-badge);
  color: #6b7280;
}

/* ====================== B 版 · 顶部工具条（覆盖全局 .filter-bar） ====================== */
/* 原 4 行筛选 → 1 行：状态 chips 横向滚动 + 「筛选」按钮靠右 */
.filter-bar {
  display: flex;
  align-items: center;
  gap: var(--space-1-5);
  margin-bottom: var(--space-2);
}
.filter-bar .chip-scroll {
  flex: 1;
  min-width: 0;
  white-space: nowrap;
}
.filter-bar .chip-group {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1-5);
}
/* 覆盖全局 .filter-bar .chip 的 flex:1（横向滚动下应为内容宽度） */
.filter-bar .chip-group .chip {
  flex: 0 0 auto;
  padding: 14rpx 26rpx;
}
.filter-toggle {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  gap: 6rpx;
  padding: 14rpx 22rpx;
  background: #1a1a1e;
  border: 1rpx solid rgba(255,255,255,0.10);
  border-radius: var(--radius-md);
  color: #c3ccd9;
  font-size: var(--fs-label);
}
.filter-toggle--active {
  border-color: rgba(200,169,81,0.5);
  background: rgba(200,169,81,0.12);
  color: #e8eef6;
}
.ft-text { font-weight: 600; }
.filter-badge {
  min-width: 30rpx;
  height: 30rpx;
  padding: 0 var(--space-1);
  border-radius: var(--radius-pill);
  background: var(--dota-gold, #C8A951);
  color: #121015;
  font-size: var(--fs-micro);
  font-weight: 800;
  line-height: 30rpx;
  text-align: center;
}

/* ====================== B-1 筛选抽屉 ====================== */
.fp-mask {
  position: fixed;
  left: 0; top: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.55);
  z-index: 100;
  display: flex;
  align-items: flex-end;
}
.fp-sheet {
  width: 100%;
  max-height: 80vh;
  background: var(--card-bg, #1a1a1e);
  border-radius: 28rpx 28rpx 0 0;
  padding: var(--space-3-5) var(--space-3-5) calc(28rpx + env(safe-area-inset-bottom));
  display: flex;
  flex-direction: column;
  box-shadow: 0 -8rpx 32rpx rgba(0,0,0,.4);
}
.fp-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-1);
}
.fp-title { font-size: var(--fs-heading); font-weight: 700; color: #f0f3f6; }
.fp-reset { font-size: var(--fs-label); color: var(--dota-gold, #C8A951); }
.fp-section {
  padding: var(--space-2-5) 0;
  border-bottom: 1rpx solid rgba(255,255,255,0.05);
}
.fp-label {
  display: block;
  font-size: var(--fs-body);
  color: #8a8f99;
  margin-bottom: 14rpx;
  font-weight: 600;
}
.gc-row {
  display: flex;
  gap: 14rpx;
  flex-wrap: wrap;
}
/* 抽屉内等级芯片：复用 .gc 视觉，缩窄高度适配抽屉 */
.fp-section .gc {
  height: 58rpx;
  padding: 0 var(--space-3);
}
.seg-row {
  display: flex;
  gap: 14rpx;
}
.seg {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-1);
  flex: 1;
  padding: var(--space-2) 0;
  border-radius: 14rpx;
  background: #1a1a1e;
  border: 1rpx solid rgba(255,255,255,0.08);
  color: #9aa4b2;
  font-size: var(--fs-label);
}
.seg--active {
  background: rgba(200,169,81,0.16);
  border-color: rgba(200,169,81,0.5);
  color: #e8eef6;
}
.tf-hint {
  display: block;
  margin-top: var(--space-1-5);
  font-size: var(--fs-caption);
  color: #6b7280;
}
.fp-foot { margin-top: var(--space-3); }
.fp-btn {
  height: 84rpx;
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--fs-emphasis);
  font-weight: 700;
}
.fp-btn--confirm {
  background: var(--dota-gold, #C8A951);
  color: #121015;
}

/* ====================== B-2 列表表格化（压缩密度） ====================== */
.league-item {
  padding: 18rpx var(--space-2-5) 18rpx var(--space-3-5);
  margin-bottom: var(--space-1-5);
}
.league-sub { margin-top: 6rpx; }
.league-countdown { margin-top: 6rpx; }
/* 来源并入 sub 行，避免独立一行 */
.src-inline {
  margin-left: var(--space-1);
  padding-left: 10rpx;
  border-left: 1rpx solid rgba(255,255,255,0.12);
  font-size: var(--fs-micro);
  color: #6b7280;
}
.src-curation { color: #C8A951; }
.src-liquipedia { color: #9bb0c9; }
.src-stratz { color: #5fd3a8; }
.src-opendota { color: #7d8794; }
